<h1>Add a new dose</h1>

<%= simple_form_for [@dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit "Add a dose", class: "btn btn-primary" %>
<% end %>

<%= link_to 'Back to all cocktails', '/cocktails' %>
